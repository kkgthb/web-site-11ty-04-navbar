        <ul class="grdGrid -top -left">
          {%- for link in links -%}
            {%- if link.sublinks == blank or link.sublinks.size == 0 -%}
              {% include 'components/nav/nav_l1link_lists', link: link, pageurl: pageurl %}
            {%- else -%}
              {% include 'components/nav/nav_dropdown_lists', link: link, pageurl: pageurl %}
            {%- endif -%}
          {%- endfor -%}
        </ul>{%- comment -%}End ul.grdGrid.-top.-left{%- endcomment -%}